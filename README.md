# wEkO-Scholink Landing Page

A static HTML landing page for wEkO-Scholink. The logo is embedded directly in `index.html`.

## Open

Open `index.html` directly in a browser. No Python server or local asset path is needed.
